import { LinkedElementDirective } from './linked-element.directive';

describe('LinkedElementDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkedElementDirective();
    expect(directive).toBeTruthy();
  });
});
