import { FragmentListenerDirective } from './fragment-listener.directive';

describe('FragmentListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new FragmentListenerDirective();
    expect(directive).toBeTruthy();
  });
});
