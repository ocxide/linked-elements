import { LinkedElementsRouterDirective } from './linked-elements-router.directive';

describe('LinkedElementsRouterDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkedElementsRouterDirective();
    expect(directive).toBeTruthy();
  });
});
